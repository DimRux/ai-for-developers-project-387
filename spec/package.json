{
  "name": "booking-calendar-spec",
  "version": "1.0.0",
  "private": true,
  "description": "TypeSpec API contract for a lightweight booking calendar (owner + guest scenarios)",
  "type": "module",
  "scripts": {
    "compile": "tsp compile .",
    "watch": "tsp compile . --watch",
    "format": "tsp format \"**/*.tsp\""
  },
  "dependencies": {
    "@typespec/compiler": "^1.14.0",
    "@typespec/http": "^1.14.0",
    "@typespec/openapi": "^1.14.0",
    "@typespec/openapi3": "^1.14.0",
    "@typespec/rest": "^0.84.0"
  }
}
